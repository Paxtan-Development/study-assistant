<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pcchin.studyassistant">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".project.status.ProjectStatusEditActivity"></activity>
        <activity android:name=".project.status.ProjectStatusActivity" />
        <activity android:name=".project.ProjectTaskEditActivity" />
        <activity android:name=".project.ProjectEditActivity" />
        <activity android:name=".project.ProjectInfoActivity" />
        <activity android:name=".notes.NotesEditActivity" />
        <activity android:name=".notes.NotesViewActivity" />
        <activity android:name=".notes.NotesSubjectActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".project.ProjectSelectActivity" />
        <activity android:name=".notes.NotesSelectActivity" />
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pcchin.studyassistant.MainActivity" />
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>